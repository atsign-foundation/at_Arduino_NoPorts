# NoPorts PoE — 4 MB stock ESP32 (esp32_wifi_tuned)
#
# Deliberately keeps nvs, otadata, spiffs (LittleFS) and coredump at the SAME
# offsets/sizes as the Arduino default.csv that esp32_wifi flashes, so a board
# provisioned with one build keeps its config (NVS) and enrolled keys
# (LittleFS at 0x290000) when reflashed with the other.  The firmware has no
# OTA, so the two 1.25 MB OTA slots are merged into one 2.5 MB factory app —
# the 3.x core with recompiled IDF libs is larger than the 1.3 MB default slot.
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  factory,  0x10000,  0x280000,
spiffs,     data, spiffs,   0x290000, 0x160000,
coredump,   data, coredump, 0x3F0000, 0x10000,
